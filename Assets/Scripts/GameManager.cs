using UnityEngine;
using System.Collections;

public enum Jobs {None, Survios, Within, VisionaryVR, SnapChat, Heavy_Iron};

public class GameManager : MonoBehaviour {

	public int day;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
